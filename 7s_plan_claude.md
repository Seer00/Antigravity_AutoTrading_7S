# SW 개발계획서 — 세븐스플릿(7-Split) 자동투자 프로그램 (키움증권 REST API 기반)

**버전**: v0.1 (초안)
**개발 방향**: 기존 시판 세븐스플릿 프로그램(매직스플릿 등) 대비 **부가기능을 배제하고 분할매수·분할매도 핵심 로직에만 집중**한 경량 자동매매 프로그램.

---

## 0. 문서 개요

* **목적**: 세븐스플릿 전략(하나의 종목을 최대 7개 분할 단위로 나누어, 하락 시 분할매수·반등 시 분할매도하여 평균단가를 관리하는 전략)을 키움증권 REST API를 통해 자동화한다.
* **개발 원칙**: "본래 기능에만 충실" — 아래는 명시적으로 **범위에서 제외**한다.
  * 다종목 동시 포트폴리오 관리, 자동 종목 발굴/추천
  * 복잡한 리밸런싱, 세금/수수료 최적화 엔진
  * 모바일 앱, 웹 대시보드의 화려한 시각화(초기엔 최소 조회 화면/로그로 충분)
  * 레버리지, 신용/미수 거래 (전략 원칙상 배제)
  * **자동/알고리즘 손절매 로직** (전략 원칙상 배제 — 대신 분할매수를 통한 평단 관리로 대응). 단, 사용자가 직접 트리거하는 **수동 긴급 청산 기능**은 별도 안전장치로 포함한다(6번 항목 참조). 이는 전략의 "손절매를 하지 않는다"는 원칙(=시스템이 임의로 손절 판단을 내리지 않음)을 유지하되, 사용자 본인의 판단에 따른 수동 개입 수단은 남겨두는 것이다.
  * 종목 자동 발굴/추천(스크리닝 알고리즘) — 종목 선정은 전적으로 사용자의 수작업 입력에 의존한다.
* **핵심 기능만 유지**:
  1. 사용자가 수작업으로 선정한 종목(1개 이상)에 대해 각각 N단계(최대 7단계) 분할매수 자동 실행
  2. 분할 단위(넘버1~N)별 독립적인 매수단가·수량 관리
  3. 분할 단위별 목표수익률 도달 시 자동 분할매도
  4. 실시간 시세 기반 트리거 감시 및 주문 실행
  5. 매매 이력 로깅 및 상태 조회
  6. 현재 보유 중인 전 종목의 상태를 한눈에 보여주는 보유현황 표
  7. 사용자가 언제든 수동으로 실행할 수 있는 긴급 청산(전량 매도) 기능

---

## 1. 요구사항 정의

### 1.1 핵심 사용자 흐름(유스케이스)

1. 사용자는 본인이 수작업으로 선정한 종목코드를 입력하고, 최초 매수금액, 분할 단계 수(최대 7), 단계별 하락 트리거(%), 단계별 목표수익률(%)을 설정한다. 이 설정은 종목별로 여러 개 등록할 수 있다(단, 종목 선정 자체를 프로그램이 추천/자동 발굴하지는 않는다).
2. 프로그램은 최초 매수(넘버1)를 실행하고, 이후 가격이 설정된 하락률에 도달할 때마다 다음 단계(넘버2, 3, …)를 자동 매수한다.
3. 각 단계는 독립적으로 목표수익률에 도달하면 해당 단계 수량만 자동 매도한다.
4. 매도된 단계는 다시 "대기" 상태로 돌아가 동일한 규칙으로 재매수 대상이 될 수 있다(설정에 따라 옵션).
5. 사용자는 현재 각 단계별 상태(매수단가, 보유수량, 평가손익, 목표가까지 잔여폭)를 조회할 수 있다.
6. 사용자는 현재 보유 중인 전체 종목을 하나의 표로 모아, 종목별 총 보유수량·평균단가·평가손익·현재가·단계 진행 현황을 한눈에 확인할 수 있다.
7. 사용자는 특정 종목(또는 전체 종목)에 대해 자동 로직과 무관하게 즉시 전량 매도를 실행하는 긴급 청산 기능을 언제든 수동으로 호출할 수 있다. 이 기능은 시세 급락, 시스템 오작동 의심 등 예외 상황에 대한 사용자 통제 수단이며, 자동 트리거 로직(원칙상 손절매 없음)과는 별개로 동작한다.

### 1.2 기능 요구사항(MVP)

* 분할 설정 등록/수정(종목은 사용자가 직접 입력, 단계 수, 단계별 트리거 %, 목표수익률 %, 단계별 투입금액) — 종목별로 복수 설정 가능
* 실시간 시세 감시 → 트리거 조건 충족 시 자동 매수/매도 주문 실행
* 계좌 잔고/보유종목/체결내역 조회(키움 API 연동)
* 단계별 상태 저장 및 조회(매수단가, 수량, 상태: 대기/보유/매도완료)
* **보유현황 표**: 현재 보유 중인 전 종목을 한 화면에서 조회(종목명, 총 보유수량, 평균단가, 현재가, 평가손익률, 진행 중 단계 수)
* **긴급 청산 기능**: 특정 종목 또는 전체 종목에 대해 자동 트리거 로직을 우회하여 사용자가 즉시 전량 매도(시장가) 주문을 실행할 수 있는 수동 제어 기능
* 매매 로그 기록(주문 요청/응답/체결 결과, 긴급 청산 실행 이력 포함)
* 모의투자 계좌를 통한 사전 검증 모드 지원

### 1.3 비범위(명시)

* 다종목 포트폴리오 **자동** 배분/리밸런싱(단, 사용자가 종목별로 수작업 등록한 분할 설정을 **병렬로 여러 개 실행**하는 것은 범위 안에 포함됨 — "자동 배분"과는 구분)
* AI 기반 종목 추천/타이밍 예측, 자동 종목 발굴/스크리닝
* 세금 계산 자동화(단, 로드맵 언급 수준으로만 남김)
* 자동/알고리즘 기반 손절매 판단(단, 사용자가 수동으로 호출하는 긴급 청산 기능은 포함됨 — 6절 참조)

---

## 2. 세븐스플릿 단순화 설계 방향

### 2.1 "7개 실물 계좌" 대신 "1개 계좌 + 내부 넘버링"

기존 세븐스플릿 프로그램은 매수/매도를 별도의 자아로 관리하기 위해 여러 계좌를 물리적으로 나누는 방식을 쓰기도 하지만, 이는 계좌 개설/자금 이체 관리 부담이 크다. 본 프로그램은 **키움증권 계좌 1개**를 사용하되, 프로그램 내부 DB에서 **넘버1~N의 매수단가/수량/상태를 가상 계좌처럼 독립 관리**하는 방식으로 단순화한다. 이는 실제 증권사 잔고(평단가 기준 표시)와 프로그램 내부 기록(단계별 기준 표시)이 다를 수 있음을 의미하며, 이 차이는 UI 설계 시 명확히 안내한다.

### 2.2 트리거 로직 단순화

* **매수 트리거**: 직전 매수단가(또는 최초 매수단가) 대비 설정된 하락률(%) 도달 시 다음 단계 매수. 단계별로 동일한 %를 쓸 수도 있고, 단계마다 다른 %를 설정할 수도 있으나 MVP에서는 **전 단계 동일 % 적용(설정 1개 값)**으로 단순화 후, 필요 시 v2에서 단계별 개별 설정으로 확장한다.
* **매도 트리거**: 각 단계의 매수단가 대비 설정된 목표수익률(%) 도달 시 해당 단계만 매도. 전 단계 동일 목표수익률로 시작한다.
* **레버리지/신용 사용 안 함**, **자동 손절매 로직 없음** — 세븐스플릿 원칙을 그대로 코드 레벨 제약으로 강제한다(신용주문 API 호출 자체를 코드에서 배제하며, Trigger Engine에는 하락률 기반 매도 조건을 두지 않는다). 다만 사용자가 직접 호출하는 긴급 청산 기능(3.3절)은 Trigger Engine을 거치지 않는 별도 경로로 두어, "시스템의 자동 손절 판단"과 "사용자의 수동 개입"을 명확히 분리한다.

---

## 3. 시스템 아키텍처

### 3.1 논리 구성요소

* **Config Manager**: 종목/단계 수/트리거%/목표수익률 등 설정 저장·수정
* **Market Watcher**: 키움 WebSocket을 통해 대상 종목 실시간 체결가 수신
* **Trigger Engine**: 수신 시세와 현재 단계별 상태를 비교해 매수/매도 조건 판단
* **Order Executor**: 조건 충족 시 키움 REST API로 매수/매도 주문 전송, 응답 처리
* **State Store(DB)**: 단계별 상태(대기/보유/매도완료), 매수단가, 수량, 주문/체결 이력 저장
* **Kiwoom API Client**: 인증(토큰 발급/갱신), 시세조회, 잔고조회, 주문 API 래핑
* **Logger/Notifier**: 매매 로그 기록, 필요 시 알림(텍스트 로그 수준으로 단순화, 알림 채널 확장은 v2)
* **Holdings View**: State Store의 종목별 단계 상태를 집계해 보유현황 표(종목명, 총 보유수량, 평균단가, 현재가, 평가손익률, 진행 단계 수)를 구성
* **Emergency Control Handler**: 사용자가 특정 종목 또는 전체 종목에 대해 즉시 전량 매도를 요청하면, Trigger Engine을 거치지 않고 곧바로 Order Executor에 시장가 매도 지시를 전달하는 별도 수동 제어 경로

### 3.2 데이터 흐름

1. Config Manager → Trigger Engine: 종목/단계 설정 로드
2. Market Watcher(WebSocket) → Trigger Engine: 실시간 체결가 스트리밍
3. Trigger Engine → State Store: 현재 단계별 상태 조회
4. 조건 충족 시 Trigger Engine → Order Executor: 매수/매도 지시
5. Order Executor → Kiwoom API Client(REST) → 키움 서버: 주문 전송
6. Kiwoom API Client → Order Executor: 체결/거부 응답
7. Order Executor → State Store: 단계 상태 갱신(대기→보유, 보유→매도완료)
8. Logger: 전 과정 기록

### 3.3 긴급 청산 흐름(수동 경로)

1. 사용자 → Emergency Control Handler: 특정 종목(또는 전체) 즉시 전량 매도 요청
2. Emergency Control Handler → Order Executor: Trigger Engine을 우회하여 시장가 매도 지시 즉시 전달(하락률/목표수익률 조건 판단 없이 실행)
3. Order Executor → Kiwoom API Client(REST): 매도 주문 전송
4. 체결 완료 시 State Store의 관련 단계 전체를 "매도완료(긴급청산)" 상태로 일괄 갱신
5. Logger: 긴급 청산 실행 사유·시각·대상 종목을 별도 이력으로 기록(사후 검증용)

이 경로는 자동 트리거 로직(2.2절)과 독립적으로 동작하며, 세븐스플릿 전략이 배제하는 "시스템의 자동 손절 판단"이 아니라 "사용자 본인의 수동 개입"이라는 점을 명확히 한다.

---

## 4. 키움증권 REST API 연동 설계

키움증권은 기존 OpenAPI+(OCX 기반, 32bit, Windows 전용) 외에 **REST + WebSocket 방식의 신규 API**를 제공한다. 인증 방식은 앱키(App Key)·시크릿(Secret Key)으로 접근토큰을 발급받아 API를 호출하는 구조로, 한국투자증권 KIS API와 유사한 흐름을 가진다.

> **주의**: 정확한 도메인, 엔드포인트 경로, TR(api-id) 코드, 토큰 만료 시간, 요청 제한(TR limit) 수치는 버전에 따라 달라질 수 있으므로, 실제 구현 전 반드시 [키움 REST API 공식 개발자 문서](https://openapi.kiwoom.com/)를 기준으로 확정해야 한다. 본 설계서는 구조적 설계 원칙만 제시한다.

### 4.1 인증 흐름

1. 키움 REST API 개발자 페이지에서 사용 신청(실전/모의 계좌 별도 신청 필요)
2. 앱키(App Key) + 시크릿(Secret Key) 발급받아 안전하게 보관(코드에 하드코딩 금지, 환경변수/시크릿매니저 사용)
3. 앱키/시크릿으로 접근토큰(Access Token) 발급 요청 → `Authorization: Bearer {token}` 헤더로 이후 모든 REST 요청에 사용
4. 토큰 만료 전 자동 갱신 로직 구현(토큰 만료시간을 API Client 내부에서 추적)

### 4.2 필요한 API(TR) 목록

| 구분 | 용도 | 비고 |
|---|---|---|
| 인증(토큰 발급/갱신) | 접근토큰 획득 | 모든 API 호출의 선행 조건 |
| 주식 현재가 조회 | 시세 폴링(WebSocket 미사용 구간 보완) | |
| 실시간 체결가 (WebSocket) | Trigger Engine의 핵심 입력 | 실시간 감시 필수 |
| 계좌 잔고/보유종목 조회 | 실제 보유수량 검증(내부 State Store와 대사) | |
| 매수 주문 | 분할매수 실행 | 신용/미수 주문 옵션 사용 금지 |
| 매도 주문 | 분할매도 실행 | |
| 주문/체결 내역 조회 | 체결 확인 및 로깅 | 연속조회 시 cont-yn/next-key 헤더 처리 필요 |

### 4.3 요청 제한(TR limit) 대응 설계

키움 REST API는 TR별 호출 횟수 제한이 존재하므로, Trigger Engine이 지나치게 빈번하게 폴링하지 않도록 **실시간 감시는 WebSocket으로, 상태 검증(잔고 대사)은 저빈도 REST 폴링(예: 수 분 간격)** 으로 역할을 분리한다. REST 호출 실패 시 재시도 백오프 로직을 공통 API Client 계층에 둔다.

### 4.4 모의투자 우선 검증

* 실전 계좌 연동 전, 반드시 **모의투자 계좌(별도 앱키/시크릿)** 로 전체 매수/매도 트리거 로직을 검증한다.
* MVP 완료 기준에 "모의투자 환경에서 최소 1개 종목, 3단계 이상 분할매수·매도 사이클을 오류 없이 완주"를 포함한다.

---

## 5. 데이터 모델(초안)

* `split_config` (분할 설정): 종목코드, 최대 단계 수, 단계별 하락 트리거(%), 목표수익률(%), 단계별 투입금액, 재매수 허용 여부
* `split_state` (단계별 상태): config_id, 단계번호(1~N), 상태(대기/보유/매도완료), 매수단가, 보유수량, 매수일시
* `order_log` (주문 이력): split_state_id, 주문유형(매수/매도), 주문가/체결가, 주문수량/체결수량, 주문시각, 키움 API 응답코드
* `token_session` (인증 세션): 앱키 식별, 발급된 접근토큰(암호화 저장), 만료시각
* `emergency_liquidation_log` (긴급 청산 이력): 대상 종목코드(또는 전체 여부), 실행시각, 실행 사유(사용자 입력, 선택), 매도 주문 결과, 청산 전/후 보유수량

> **참고**: 보유현황 표는 별도 테이블이 아니라 `split_state`를 종목코드 기준으로 집계(SUM/AVG)한 뷰(View)로 구성한다.

---

## 6. 비기능 요구사항(NFR)

* **안정성**: WebSocket 연결 끊김 시 자동 재연결, REST 호출 실패 시 재시도(최대 N회) 및 실패 로깅.
* **일관성**: 내부 State Store 상태와 실제 키움 계좌 잔고를 주기적으로 대사(reconciliation)하여 불일치 시 경고 로그 생성.
* **보안**: 앱키/시크릿/접근토큰은 평문 저장 금지(환경변수 또는 로컬 암호화 저장소 사용).
* **단순성 우선**: UI는 최소한의 상태 조회 화면(CLI 또는 단순 웹 대시보드)으로 시작하고, 화려한 시각화는 배제한다.
* **감사 추적성**: 모든 자동 주문은 트리거 조건(어떤 규칙으로 발생했는지)을 로그에 함께 남겨 사후 검증 가능하게 한다.
* **긴급 기능의 즉시성**: 긴급 청산 요청은 Trigger Engine의 대기열이나 폴링 주기와 무관하게 최우선으로 처리되어야 하며, 실행 지연이 발생하지 않도록 별도 경로(3.3절)로 분리한다.

---

## 7. 리스크/이슈 목록

1. **API 사양 변경 리스크**: 키움 REST API는 비교적 최근 출시된 신규 인터페이스로, 엔드포인트/TR 코드/요청 제한이 변경될 수 있음 → API Client를 별도 계층으로 격리해 사양 변경 시 이 계층만 수정하면 되도록 설계.
2. **손절매 부재로 인한 무한 물타기 리스크**: 세븐스플릿 원칙상 손절매를 하지 않으므로, 종목이 구조적으로 하락(상장폐지, 산업 붕괴 등)할 경우 자금이 묶이거나 손실이 커질 수 있음. 이는 전략 자체의 한계이며, 프로그램 레벨에서는 **최대 투입 가능 총액(전체 한도) 설정**을 강제하여 리스크를 제한하는 안전장치를 둔다.
3. **실시간 데이터 유실**: WebSocket 연결 문제로 트리거 조건을 놓칠 경우 REST 폴링으로 보완하되, 완전한 실시간성은 보장되지 않을 수 있음.
4. **모의투자와 실전 계좌 간 동작 차이**: 모의투자 환경에서 검증된 로직이 실전 환경(체결 지연, 슬리피지 등)에서 다르게 동작할 수 있음 → 실전 전환 초기엔 소액/저단계 수로 제한 운영 권장.
5. **긴급 청산 기능의 오작동/오남용 리스크**: 자동 로직을 우회하는 만큼, 잘못된 종목 선택이나 UI 조작 실수로 의도치 않은 전량 매도가 발생할 수 있음 → 실행 전 확인(재확인 프롬프트) 절차를 두고, 실행 이력을 반드시 별도 로그로 남긴다.

---

## 8. 개발 로드맵(단계별)

1. **1단계**: 키움 REST API 인증(토큰 발급/갱신) 모듈 + 모의투자 연동 검증
2. **2단계**: 계좌 잔고/시세 조회 API 연동 (State Store 대사 포함)
3. **3단계**: 분할 설정(Config) 등록/조회 기능 구현(종목 수작업 입력, 종목별 복수 설정 지원)
4. **4단계**: WebSocket 기반 실시간 시세 감시 + Trigger Engine 구현(매수 트리거만 우선)
5. **5단계**: 매도 트리거 구현 + 단계별 상태 전환 로직 완성
6. **6단계**: 보유현황 표(Holdings View) 및 긴급 청산 기능(Emergency Control Handler) 구현
7. **7단계**: 모의투자 환경에서 1개 이상 종목 전체 사이클(매수→매도→재매수) 및 긴급 청산 시나리오 검증
8. **8단계**: 실전 계좌 전환(소액/저단계 수로 제한 운영) + 로깅/모니터링 보완
9. **차기 로드맵(범위 밖)**: 단계별 개별 트리거 설정, 알림 채널 확장(긴급 청산 발생 시 알림 등), 세금 계산 자동화, 자동 종목 발굴/추천